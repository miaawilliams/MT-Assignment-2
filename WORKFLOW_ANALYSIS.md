1. 
    - The workflow is triggered when the code is pushed to the main branch

2. 
    - Step 1: Get the code from the repositroy
    - Step 2: Validate the HTML files
    - Step 3: Check for broken links
    - Step 4: Upload the built site for deployment

3. 
    - The checkout code is what pulls the code from the repository into the codespace

4.
    - It sets up the right setting and variables that the program needs for it to run correctly

5.
    - The automatic deployment allows for a quicker setup versus a manual deployment. With an automatic deployment,
    I can start working right away instead of setting up my space, making sure everything is running right, and then getting to work.

6.
    - If I pushed the code to another branch other than the main branch, it wouldn't change the final production. It would change the specific branch that you worked on.